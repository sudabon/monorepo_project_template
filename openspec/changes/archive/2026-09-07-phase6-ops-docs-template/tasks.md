## 1. README と立ち上げチェックリスト

- [x] 1.1 テンプレート固有の識別子(モジュールパス等)を洗い出し、全文検索で一意に見つけられる状態になっていることを確認する
- [x] 1.2 `README.md` を新規案件での使い方として書き直し、チェックリストと最初に実行するコマンドを先頭に配置する
- [x] 1.3 チェックリストにモジュールパス、パッケージ名、サンプルリソース名、CI のシークレット名、`.openspec-e2e-kit.json` の `templateRepo` フラグ解除を含め、5 項目すべてが記載されていることを確認する
- [x] 1.4 チェックリスト適用後の検証手順(テンプレート識別子の全文検索。ドキュメント内の言及は除外)を README に記載する
- [x] 1.5 実際にテンプレートを複製してチェックリストを適用し、識別子が残らないこと、および `make setup && make check` が成功することを確認する
- [x] 1.6 複製先で `templateRepo` フラグを解除した状態で、test-plan.md を持つ change に対応するタグ付きテストがない場合に `scripts/check-test-plan.sh` が失敗することを確認する（テンプレート側で無効化されていた検証が複製先で有効になっている）
- [x] 1.7 `templateRepo` フラグを解除しないままチェックリストの検証手順(全文検索)を実行した場合に、解除漏れが検出されることを確認する
- [x] 1.8 設計の説明を README に書かず `docs/` と ADR へのリンクに留めていることをレビューで確認する

## 2. STACK / UPGRADING

- [x] 2.1 `docs/STACK.md` を、依存宣言に含まれる主要ライブラリの採用バージョン・選定理由・次のメジャーで壊れそうな点の一覧として再構成し、依存宣言との突き合わせで漏れがないことを確認する
- [x] 2.2 `docs/UPGRADING.md` に React / Tailwind / TanStack Router / Go / Echo のメジャー更新手順を記載する
- [x] 2.3 `docs/UPGRADING.md` に、実施時に判明した問題を書き足す場所である旨と追記の書式を明記する

## 3. RUNBOOK

- [x] 3.1 Aurora のマイナー / メジャーアップグレード手順を `docs/RUNBOOK.md` に記載する
- [x] 3.2 Terraform provider のメジャー更新手順を記載する
- [x] 3.3 依存更新 PR のレビュー基準を「本番配信物に入るか否か」の軸で記載し、ビルド時ツールのサプライチェーンリスクという別軸も併記する
- [x] 3.4 `npm audit` 警告の切り分け方針を記載し、devDependencies の脆弱性が本番配信物に届かないことと、それが「無視してよい」ではないことを明記する
- [x] 3.5 障害時の初動(ログの見方、リクエスト ID による追跡手順)を記載し、実際にローカルでリクエスト ID からログを特定できることを手順どおりに実行して確認する
- [x] 3.6 Phase 4 で用意した監視サービス送信フックの差し込み方を記載する(Phase 4 で記載済みなら整合を取る)

## 4. ADR

- [x] 4.1 各フェーズの design.md の Decisions から、覆すコストが高い判断を選別して ADR 化する対象を決定する
- [x] 4.2 契約を手書きにする判断、認証を BFF に閉じる判断、環境設定を実行時 fetch にする判断、UI をコピー型にする判断、層構造と依存方向の判断を、それぞれ判断・理由・代替案を含む ADR として `docs/adr/` に記録する
- [x] 4.3 各フェーズで既に ADR 化済みの項目と重複していないことを確認し、重複があれば参照に置き換える
- [x] 4.4 覆された判断は削除せず後続 ADR で上書きする運用を `docs/adr/` の冒頭または README に明記する

## 5. AGENTS.md

- [x] 5.1 ルート `AGENTS.md` に共通ルールと、作業対象に応じてどの階層の指示を読むかの案内のみを記載し、バックエンド固有の文脈を含まないことを確認する
- [x] 5.2 `apps/api/AGENTS.md` / `apps/bff/AGENTS.md` / `apps/web/AGENTS.md` の内容を確認し、各フェーズで作成済みのものと整合を取る
- [x] 5.3 ルートと階層側でルールが重複・矛盾していないことを確認し、重複は階層側を正として解消する
- [x] 5.4 フロントエンドの作業を Coding Agent に指示し、Go の文脈を読まずに作業できることを確認する

## 6. 配信要件

- [x] 6.1 `index.html` を no-cache にする要件と、守られない場合に何が起きるかを `docs/` に記載する
- [x] 6.2 内容ハッシュ付きアセットを immutable として長期キャッシュする要件と理由を記載する
- [x] 6.3 403/404 を `/index.html` に 200 で fallback する要件と理由(SPA のルーティング)を記載する
- [x] 6.4 `config.json` も no-cache が必要であり、ハッシュ付きアセットと同じ扱いにしてはならない旨を記載し、Phase 4 の記載と整合していることを確認する

## 7. テンプレート化と完了条件の検証

- [x] 7.1 リポジトリを GitHub の Template repository として設定できる状態(不要な履歴・案件固有の値が残っていない)であることを確認する
- [x] 7.2 README のみを読んで新規案件を立ち上げる手順を通しで実行し、所要時間を計測して 30 分以内に収まることを確認する
- [x] 7.3 `make check` が全て通ることを確認する
- [x] 7.4 全フェーズの `// TODO(template):` を集約し、テンプレートとして残すもの・解消するものを仕分けて報告する

## E2Eテスト実装タスク(必須)

本 change は E2E 不要のため test-plan.md を持たない(成果物はドキュメントとテンプレート化であり、
ブラウザから観測できるアプリケーションの振る舞いを変更しないため)。したがって
`@phase6-ops-docs-template` タグを持つ E2E テストは実装しない。各シナリオの検証はレビューと、
テンプレートを実際に複製して手順を適用する手動検証で行い、タスク 1〜7 で指定している。
