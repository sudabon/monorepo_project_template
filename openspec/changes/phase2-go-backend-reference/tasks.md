## 1. 基盤の選定と骨格

- [x] 1.1 マイグレーションツールを D7 の基準で選定し、選定理由と代替案を `docs/adr/` に記録する
- [x] 1.2 依存方向検査ツールを D2 の基準で選定し、比較結果を `docs/adr/` に記録する
- [x] 1.3 統合テストの DB 起動手段(testcontainers / CI サービスコンテナ)を選定し、ローカルと CI の双方で同じ `make` ターゲットから実行できることを確認する
- [x] 1.4 `apps/api/` に handler / usecase / domain / repository の 4 層のディレクトリを作成し、`go build ./...` が通ることを確認する

## 2. データベースとマイグレーション

- [x] 2.1 サンプルリソースのテーブル定義をマイグレーションとして追加し、適用とロールバックの両方が成功することを確認する
- [x] 2.2 `Makefile` にマイグレーション適用・ロールバックのターゲットを追加し、ローカルから実行できることを確認する
- [x] 2.3 DB 接続の初期化とコネクションプール設定を実装し、設定値の根拠をコードコメントに記す

## 3. サンプルリソースの実装

- [x] 3.1 domain 層にサンプルリソースの型とビジネスルール、repository インタフェースを定義し、外側の層へ依存していないことを `make lint` で確認する
- [x] 3.2 repository 実装(DB アクセス)を追加し、実 DB に対する統合テストで作成・取得・更新・削除・一覧が通ることを確認する
- [x] 3.3 usecase 層に 5 操作を実装し、repository をモックしたユニットテストが通ることを確認する
- [x] 3.4 handler 層で生成された Echo サーバインタフェースを実装し、インタフェース型への束縛によって実装漏れがビルドエラーになることを、1 操作を一時的に外して確認する
- [x] 3.5 生成型と domain 型の変換を handler 層に閉じ、domain が生成型に依存していないことを `make lint` で確認する
- [x] 3.6 一覧取得のページネーションを実装し、複数ページにまたがるデータで重複・欠落がないことを統合テストで確認する
- [x] 3.7 空の一覧がエラーではなく空集合として返ることを統合テストで確認する

## 4. 入力検証とエラー応答

- [x] 4.1 契約の制約に基づく入力検証を実装し、単一フィールド違反が契約のバリデーションエラー形式で返ることを統合テストで確認する
- [x] 4.2 複数フィールドの違反が 1 回の応答にまとめて返ることを統合テストで確認する
- [x] 4.3 検証エラー時に永続化されないことを、エラー後の一覧取得で確認する統合テストを追加する
- [x] 4.4 存在しない識別子での取得・更新・削除がリソース不在の応答になり、内部エラーにならないことを統合テストで確認する
- [x] 4.5 ユーザ承認済みの NUL（U+0000）禁止を契約と文書に明記し、作成・更新が 422 のフィールドエラーとなり永続化されないことを検証する

## 5. 実行時の振る舞い

- [x] 5.1 リクエスト ID のヘッダ名を決定し(Phase 3 の BFF が従うため design.md に明記)、ミドルウェアでヘッダからの引き継ぎまたは生成を実装する
- [x] 5.2 `log/slog` による構造化ログを設定し、`context` 経由でリクエスト ID がすべてのログに含まれることを、ログ出力を捕捉するテストで確認する
- [x] 5.3 エラー応答からリクエスト ID を得られるようにし、エラーログの ID と一致することをテストで確認する
- [x] 5.4 shallow ヘルスチェックを実装し、DB を停止した構成でも正常応答することをテストで確認する
- [x] 5.5 deep ヘルスチェックを実装し、依存先ごとの判定結果を含む応答になること、DB 停止時に異常応答することをテストで確認する
- [x] 5.6 graceful shutdown を実装し、処理中リクエストの完了後に終了することを SIGTERM 送出テストで確認する
- [x] 5.7 待機上限を環境変数で設定可能にし、上限到達時に終了しログに記録されることをテストで確認する。ECS の `stopTimeout` との関係をコードコメントに記す

## 6. CI と完了条件の検証

- [ ] 6.1 `.github/workflows/go.yml` に DB を伴う統合テストの実行環境を追加し、CI で `make test` が成功することを確認する
- [x] 6.2 `make check` が全て通ることを確認する
- [x] 6.3 ローカルで起動し、サンプルリソースの CRUD を curl で一通り実行できることを確認する
- [x] 6.4 妥協した実装に `// TODO(template):` を付け、一覧を報告に含める
- [x] 6.5 `apps/api/AGENTS.md` に層の責務と依存方向の規約を記述する

## E2Eテスト実装タスク(必須)

本 change は E2E 不要のため test-plan.md を持たない(画面が存在せず、HTTP レベルの振る舞いは
Go 統合テストで検証するため。Playwright のスモークはテンプレート方針として Phase 5 の 1 本のみと
する)。したがって `@phase2-go-backend-reference` タグを持つ E2E テストは実装しない。各シナリオの
検証は Go のユニットテスト・統合テスト(実 DB / SIGTERM 送出 / ログ捕捉)で行い、タスク 3〜5 で
指定している。
